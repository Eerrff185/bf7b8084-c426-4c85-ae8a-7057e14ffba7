name: Decompile Assembly-CSharp-CountFiveandDie.dll

on:
  workflow_dispatch:

jobs:
  decompile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Install ILSpy CLI
        run: dotnet tool install --global ilspycmd

      - name: Decompile Assembly-CSharp-CountFiveandDie.dll
        run: |
          mkdir output
          ~/.dotnet/tools/ilspycmd Assembly-CSharp-CountFiveandDie.dll -p -o output

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: Assembly-CSharp-CountFiveandDie.dll
          path: output
